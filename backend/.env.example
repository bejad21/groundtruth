GEMINI_API_KEY=
GEMINI_MODEL=gemini-flash-latest
GEMINI_MODEL_FALLBACK=gemini-flash-lite-latest
OPENROUTER_API_KEY=
OPENROUTER_MODEL=nvidia/nemotron-nano-12b-v2-vl:free
OPENROUTER_MODEL_FALLBACK=google/gemma-4-31b-it:free
CORS_ORIGINS=http://localhost:5173
CONFIDENCE_REVIEW_THRESHOLD=0.75

# Bearer token clients must send as `Authorization: Bearer <key>`. Generate with:
#   python -c "import secrets; print(secrets.token_urlsafe(32))"
DEMO_API_KEY=

# Encrypts source_name, truck_or_driver_id, and notes at rest. Generate with:
#   python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
ENCRYPTION_KEY=
